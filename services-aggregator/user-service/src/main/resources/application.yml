spring:
  config:
    import:
      - application-sql.yml
---
spring:
  datasource:
    hikari:
      pool-name: HikariUserServiceConnection #sadece logda okumak için
      schema: user_schema

server:
  port: 9090